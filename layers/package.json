{
  "name": "layers",
  "version": "0.0.0",
  "scripts": {
    "chrome": "git clone --depth=1 https://github.com/alixaxel/chrome-aws-lambda.git && cd chrome-aws-lambda && make ../chrome_aws_lambda.zip && cd .. && rm -rf chrome-aws-lambda"
  }
}
